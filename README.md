# 🏗️ AutoCad Web Viewer v2.0

مشروع متكامل لعرض ملفات AutoCAD (DWG) على الويب باستخدام تقنيات حديثة. يتميز المشروع ببنية برمجية احترافية في الباك أند (Clean Architecture) وتحكم كامل ورؤية ثلاثية الأبعاد في الفرونت أند.

---

## 🚀 نظرة عامة على المشروع

يسمح هذا التطبيق للمستخدمين برفع ملفات DWG، تحليلها في خادم ASP.NET Core، وعرض محتوياتها الهندسية (Lines, Circles, Arcs) في المتصفح باستخدام محرك Three.js.

### 🌟 المميزات الرئيسية
- **دعم الأشكال الهندسية:** عرض الخطوط، الدوائر، والأقواس بدقة عالية.
- **تفاعل كامل:** عمل Zoom و Pan وتحديد العناصر (Select) لرؤية خصائصها.
- **البحث المتقدم:** البحث عن أي عنصر داخل الرسمة باستخدام الـ Handle ID الخاص به.
- **خصائص تفصيلية:** عرض كافة metadata العناصر (الطبقات، الألوان، الأبعاد، المساحات).
- **تصميم عصري:** واجهة Dashboard مع Sidebar ثابت للتحكم والخصائص.

---

## 📂 هيكل المشروع (Solution Structure)

```text
AutoCadViewr
├── 🌐 GWDViewer            # الفرونت أند (Frontend)
│   ├── index.html          # الهيكل والواجهة
│   ├── styles.css          # التصميم الحديث ونظام الـ Sidebar
│   └── viewer.js           # منطق Three.js والتفاعل مع الـ API
│
└── ⚙️ DWGViewerAPI         # الباك أند (Backend - ASP.NET Core)
    ├── 🛠️ Infrastructure   # تحميل الملفات (AWS S3 Support)
    ├── 📦 Models           # قوالب البيانات (Strategy Pattern DTOs)
    ├── 🖇️ Services         # منطق العمل (Clean Architecture)
    │   ├── Converters      # المحولات الذكية لكل نوع عنصر
    │   └── ColorResolver   # حل الألوان (ByLayer/ByBlock)
    └── 🎮 Controllers      # الـ API Endpoints
```

---

## 🎨 الواجهة الأمامية (Frontend Details)

تعتمد الواجهة على **Three.js** و **WebGL** لتقديم أداء رسومي عالي:
- **نظام العرض:** كاميرا Orthographic لمحاكاة واجهة الأوتوكاد التقليدية.
- **Sidebar Layout:** لوحة جانبية منظمة تحتوي على:
    - **أدوات التحكم:** رفع ملف محلي أو التحميل من رابط مباشر.
    - **محرك البحث:** للوصول السريع للعناصر بـ Handle ID.
    - **لوحة الخصائص:** تعرض البيانات بشكل مصنف (Basic, Geometric, Visual).

---

## 🧬 الباك أند (Backend Architecture)

تم بناء الـ API باستخدام **Clean Architecture** و **Strategy Pattern**:

1.  **IEntityTypeConverter:** واجهة موحدة تجعل النظام قابلاً للتوسع لإضافة أي عنصر (مثل Text أو Hatch) دون تعديل الكود القديم.
2.  **ColorResolver:** لضمان ظهور الألوان تماماً كما تظهر في AutoCAD.
3.  **Dependency Injection:** جميع الخدمات والمحولات يتم حقنها ديناميكياً لضمان سهولة الاختبار والتطوير.

---

## 🛠️ كيف تبدأ (Prerequisites)

### تشغيل الباك أند
1. افتح مجلد `DWGViewerAPI`.
2. تأكد من تثبيت `.NET 8.0 SDK`.
3. قم بتشغيل الأمر: `dotnet run`.
4. افتراضياً، يعمل الـ API على `http://localhost:5183`.

### تشغيل الفرونت أند
1. افتح مجلد `GWDViewer`.
2. افتح ملف `index.html` في أي متصفح (أو استخدم Live Server).
3. تأكد من أن الـ API يعمل ليتم معالجة الملفات المرفوعة.

---

## ✅ مبادئ التصميم المتبعة
- **SOLID Principles:** تم تطبيقها بالكامل لضمان نظافة الكود.
- **Performance:** يتم تحميل الأشكال الهندسية فقط وتقليل حجم الـ JSON المرسل.
- **Scalability:** المشروع مصمم ليتحمل إضافة مئات أنواع العناصر المعقدة مستقبلاً.

---

تم التطوير بكل ❤️ لتقديم أفضل تجربة عرض لملفات الأوتوكاد على الويب.
